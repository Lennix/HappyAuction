- updated for 1.0.4
    - added haSortTimeLeft()
- fixed minor relogin bug that would delay the process by unintentionally opening options menu.
- now supports decimal numbers. so an item with 4.5 crit will be read as 4.5 not 4.
- added haPrompt(). like haAlert() but with YES/NO buttons and return status. see README.


- USD/RMAH swap support
- support new item types
- show error and kill script when haFilterStat fails. this is to prevent unwanted purchases.


- add native call support
- clear "high transaction volume" popups