- added haSetLogin() to replace haReLogin(). see README
- deprecated haReLogin(). will redirect to haSetLogin() until completely removed in the future.
- improved haBuyout/haBid performance by a few frames.
- add haItem().rarity representing item rarity. see README
- add haItem().type representing item type. see README



- seperate strings into a file allowing easy locale updates.
- show error and kill script when haFilterStat fails. this is to prevent unwanted purchases.
- add native call support
- automatically clear popups such as "high transaction volume" errors
- supports decimal values such as 7.5 instead of rounding down to 7
