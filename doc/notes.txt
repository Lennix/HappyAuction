- added haStashNext() and haStashSell() allowing you to scan your stash and sell stuff (see README).
- added haItem(), haItemStat() (see README) and deprecated haListItem(), haListItemStat(), haListItemSocket().
- updated input method. now requires d3 window to be in foreground
- greatly improved haActionReLogin(). see README and example in Bots/GemMiner.
- improve responsiveness when stopping a script
- fixed a stat filter bug sometimes causing wrong filter to be selected
- improved performance even more. now will reach input limit twice as fast! :D
- updated all sample bots to use haItem() instead.
- sample bots are now be enabled by uncommenting the corresponding require() in Main.lua.
